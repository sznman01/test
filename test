<!DOCTYPE html>
<html lang="zh-HK">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>日本旅遊規劃 - Travel Planner Pro</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-red: #BC002D;
            --accent-pink: #FFB7C5;
            --accent-green: #00A86B;
            --dark-gray: #2D3436;
            --light-bg: #F8F9FA;
            --white: #FFFFFF;
            --shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            --shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.12);
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans JP', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: var(--dark-gray);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            color: white;
            margin-bottom: 40px;
        }

        .header h1 {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .header p {
            font-size: 16px;
            opacity: 0.9;
        }

        /* 主畫面 - 旅程卡片列表 */
        .trips-container {
            display: flex;
            flex-direction: column;
            gap: 24px;
            margin-bottom: 60px;
        }

        .trip-card {
            position: relative;
            height: 280px;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: var(--shadow-lg);
            cursor: pointer;
            transition: all 0.3s ease;
            background: var(--white);
        }

        .trip-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .trip-image {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 100%;
            background-size: cover;
            background-position: center;
            opacity: 0.85;
            transition: opacity 0.3s ease;
        }

        .trip-card:hover .trip-image {
            opacity: 0.7;
        }

        .trip-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0,0,0,0.8));
            padding: 32px 24px 24px;
            color: white;
        }

        .trip-title {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 4px;
        }

        .trip-meta {
            display: flex;
            gap: 16px;
            margin-bottom: 8px;
            font-size: 14px;
            opacity: 0.9;
        }

        .trip-meta span {
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .trip-description {
            font-size: 14px;
            line-height: 1.5;
            max-height: 60px;
            overflow: hidden;
        }

        /* 新增旅程卡片 */
        .add-trip-card {
            position: relative;
            height: 200px;
            border-radius: 24px;
            background: rgba(255,255,255,0.15);
            backdrop-filter: blur(20px);
            border: 2px dashed rgba(255,255,255,0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .add-trip-card:hover {
            background: rgba(255,255,255,0.25);
            border-color: rgba(255,255,255,0.6);
            transform: scale(1.02);
        }

        .add-trip-icon {
            font-size: 48px;
            margin-bottom: 16px;
        }

        .add-trip-text {
            color: white;
            font-size: 18px;
            font-weight: 600;
        }

        /* 新增旅程 Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            background: var(--white);
            border-radius: 24px;
            padding: 40px;
            max-width: 500px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
            box-shadow: var(--shadow-lg);
        }

        .modal-header {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 24px;
            text-align: center;
            color: var(--dark-gray);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            font-size: 14px;
            font-weight: 600;
            color: var(--dark-gray);
            margin-bottom: 8px;
        }

        .form-input, .form-select, .form-textarea {
            width: 100%;
            padding: 16px;
            border: 2px solid #E8E8E8;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .form-input:focus, .form-select:focus, .form-textarea:focus {
            outline: none;
            border-color: var(--primary-red);
            box-shadow: 0 0 0 4px rgba(188, 0, 45, 0.1);
        }

        .form-textarea {
            resize: vertical;
            min-height: 100px;
        }

        .country-info {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            border-radius: 16px;
            padding: 20px;
            margin: 20px 0;
            color: white;
            text-align: center;
        }

        .country-weather {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .country-currency {
            font-size: 14px;
            opacity: 0.9;
        }

        .modal-buttons {
            display: flex;
            gap: 12px;
            margin-top: 24px;
        }

        .btn {
            flex: 1;
            padding: 16px;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: var(--primary-red);
            color: white;
        }

        .btn-primary:hover {
            background: #8B001F;
        }

        .btn-secondary {
            background: #F8F9FA;
            color: var(--dark-gray);
        }

        /* 底部導航 */
        .bottom-nav {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 12px;
        }

        .nav-btn {
            padding: 12px 24px;
            background: rgba(255,255,255,0.9);
            backdrop-filter: blur(20px);
            border-radius: 50px;
            border: none;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

        .nav-btn:hover {
            background: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }

        /* 旅程詳情頁面預覽 */
        .trip-detail-preview {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: white;
            z-index: 999;
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 28px;
            }
            
            .trip-card {
                height: 240px;
            }
            
            .trip-overlay {
                padding: 24px 20px 20px;
            }
            
            .trip-title {
                font-size: 20px;
            }
            
            .modal-content {
                margin: 20px;
                padding: 32px 24px;
            }
        }

        /* 飄落雪花特效 */
        .snowflake {
            position: fixed;
            top: -10px;
            color: white;
            font-size: 20px;
            animation: fall linear infinite;
            pointer-events: none;
            z-index: 100;
        }

        @keyframes fall {
            to {
                transform: translateY(100vh) rotate(360deg);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- 頁面標題 -->
        <div class="header">
            <h1>✈️ 我的旅遊計劃</h1>
            <p>建立和管理您的完美旅行行程</p>
        </div>

        <!-- 主畫面 - 旅程卡片列表 -->
        <div id="mainPage" class="trips-container">
            <!-- 示例旅程卡片 -->
            <div class="trip-card" onclick="enterTrip(0)">
                <div class="trip-image" style="background-image: url('https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80')"></div>
                <div class="trip-overlay">
                    <div class="trip-title">日本 2026 之旅</div>
                    <div class="trip-meta">
                        <span>🌍 日本</span>
                        <span>📅 2026/1/14 - 1/20</span>
                        <span>🕐 7天</span>
                    </div>
                    <div class="trip-description">大阪、東京、京都經典環線，美食購物文化之旅，探索日本最美景點與隱藏美食</div>
                </div>
            </div>

            <div class="trip-card" onclick="enterTrip(1)">
                <div class="trip-image" style="background-image: url('https://images.unsplash.com/photo-1572491796066-01b2b3deedb8?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80')"></div>
                <div class="trip-overlay">
                    <div class="trip-title">泰國 海島天堂</div>
                    <div class="trip-meta">
                        <span>🌴 泰國</span>
                        <span>📅 2026/3/1 - 3/8</span>
                        <span>🕐 8天</span>
                    </div>
                    <div class="trip-description">曼谷+普吉島+攀牙灣，海灘度假與叢林探險的完美結合</div>
                </div>
            </div>

            <!-- 新增旅程卡片 -->
            <div class="add-trip-card" onclick="openAddTripModal()">
                <div style="text-align: center;">
                    <div class="add-trip-icon">✨</div>
                    <div class="add-trip-text">+ 新增旅遊計劃</div>
                </div>
            </div>
        </div>
    </div>

    <!-- 底部導航 -->
    <div class="bottom-nav">
        <button class="nav-btn" onclick="scrollToTop()">🏠 首頁</button>
        <button class="nav-btn" onclick="openSettings()">⚙️ 設置</button>
    </div>

    <!-- 新增旅程 Modal -->
    <div class="modal" id="addTripModal">
        <div class="modal-content">
            <div class="modal-header">✨ 新增旅遊計劃</div>
            
            <form id="addTripForm">
                <div class="form-group">
                    <label class="form-label">國家/地區 <span style="color: red;">*</span></label>
                    <select class="form-select" id="tripCountry" required onchange="updateCountryInfo()">
                        <option value="">選擇國家</option>
                        <option value="japan">日本 🇯🇵</option>
                        <option value="thailand">泰國 🇹🇭</option>
                        <option value="korea">韓國 🇰🇷</option>
                        <option value="taiwan">台灣 🇹🇼</option>
                        <option value="europe">歐洲 🇪🇺</option>
                        <option value="usa">美國 🇺🇸</option>
                    </select>
                </div>

                <div id="countryInfo" class="country-info" style="display: none;">
                    <div class="country-weather" id="countryWeather">--°C</div>
                    <div class="country-currency" id="countryCurrency">貨幣: --</div>
                </div>

                <div class="form-group">
                    <label class="form-label">計劃名稱 <span style="color: red;">*</span></label>
                    <input type="text" class="form-input" id="tripTitle" placeholder="例如：日本2026之旅" required>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label class="form-label">開始日期</label>
                        <input type="date" class="form-input" id="tripStartDate">
                    </div>
                    <div class="form-group">
                        <label class="form-label">天數</label>
                        <input type="number" class="form-input" id="tripDays" min="1" max="30" placeholder="7">
                    </div>
                </div>

                <div class="form-group">
                    <label class="form-label">簡介描述</label>
                    <textarea class="form-textarea" id="tripDescription" placeholder="簡單描述您的旅行計劃..."></textarea>
                </div>

                <div class="form-group">
                    <label class="form-label">封面圖片（可選）</label>
                    <input type="file" class="form-input" id="tripCoverImage" accept="image/*">
                    <small style="color: #666;">上傳高質素旅行圖片作為封面</small>
                </div>

                <div class="modal-buttons">
                    <button type="submit" class="btn btn-primary">✅ 建立計劃</button>
                    <button type="button" class="btn btn-secondary" onclick="closeAddTripModal()">取消</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // 旅程數據
        let trips = [
            {
                id: 0,
                title: '日本 2026 之旅',
                country: 'japan',
                startDate: '2026-01-14',
                days: 7,
                description: '大阪、東京、京都經典環線，美食購物文化之旅，探索日本最美景點與隱藏美食',
                coverImage: 'https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=2070&q=80',
                weather: '8-12°C ☀️',
                currency: '1 JPY ≈ 0.051 HKD'
            },
            {
                id: 1,
                title: '泰國 海島天堂',
                country: 'thailand',
                startDate: '2026-03-01',
                days: 8,
                description: '曼谷+普吉島+攀牙灣，海灘度假與叢林探險的完美結合',
                coverImage: 'https://images.unsplash.com/photo-1572491796066-01b2b3deedb8?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80',
                weather: '28-33°C 🌴',
                currency: '1 THB ≈ 0.22 HKD'
            }
        ];

        // 國家資訊數據
        const countryData = {
            japan: {
                weather: '8-12°C ☀️ 隱雲時晴',
                currency: '1 JPY ≈ 0.051 HKD [web:43]',
                image: 'https://images.unsplash.com/photo-1540959733332-eab4deabeeaf?w=400'
            },
            thailand: {
                weather: '28-33°C 🌴 晴朗炎熱',
                currency: '1 THB ≈ 0.22 HKD',
                image: 'https://images.unsplash.com/photo-1572491796066-01b2b3deedb8?w=400'
            },
            korea: {
                weather: '0-5°C ❄️ 寒冷乾燥',
                currency: '1 KRW ≈ 0.0058 HKD',
                image: 'https://images.unsplash.com/photo-1558618047-3c8c76ca3350?w=400'
            },
            taiwan: {
                weather: '18-23°C ☁️ 舒適濕潤',
                currency: '1 TWD ≈ 0.24 HKD',
                image: 'https://images.unsplash.com/photo-1589879217433-6e962fbeb0bb?w=400'
            },
            europe: {
                weather: '2-8°C 🌨️ 多雲有雪',
                currency: '1 EUR ≈ 8.45 HKD',
                image: 'https://images.unsplash.com/photo-1543782249-3f2ec6195a59?w=400'
            },
            usa: {
                weather: '5-15°C ☀️ 涼爽晴朗',
                currency: '1 USD ≈ 7.80 HKD',
                image: 'https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=400'
            }
        };

        // 開啟新增旅程 Modal
        function openAddTripModal() {
            document.getElementById('addTripModal').classList.add('active');
        }

        // 關閉新增旅程 Modal
        function closeAddTripModal() {
            document.getElementById('addTripModal').classList.remove('active');
            document.getElementById('addTripForm').reset();
            document.getElementById('countryInfo').style.display = 'none';
        }

        // 更新國家資訊
        function updateCountryInfo() {
            const country = document.getElementById('tripCountry').value;
            const infoDiv = document.getElementById('countryInfo');
            
            if (country && countryData[country]) {
                const data = countryData[country];
                document.getElementById('countryWeather').textContent = data.weather;
                document.getElementById('countryCurrency').textContent = data.currency;
                infoDiv.style.display = 'block';
            } else {
                infoDiv.style.display = 'none';
            }
        }

        // 提交新增旅程
        document.getElementById('addTripForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            const country = document.getElementById('tripCountry').value;
            const title = document.getElementById('tripTitle').value;
            const startDate = document.getElementById('tripStartDate').value;
            const days = document.getElementById('tripDays').value || 7;
            const description = document.getElementById('tripDescription').value;
            const coverFile = document.getElementById('tripCoverImage').files[0];
            
            if (!country || !title) {
                alert('請填寫國家和計劃名稱！');
                return;
            }
            
            const data = countryData[country];
            const newTrip = {
                id: trips.length,
                title,
                country,
                startDate: startDate || '2026-01-01',
                days: parseInt(days),
                description: description || '',
                coverImage: data.image,
                weather: data.weather,
                currency: data.currency
            };
            
            trips.push(newTrip);
            renderTrips();
            closeAddTripModal();
            saveTrips();
            
            // 自動滾動到新添加的卡片
            setTimeout(() => {
                const newCard = document.querySelector(`[onclick="enterTrip(${newTrip.id})"]`);
                newCard.scrollIntoView({ behavior: 'smooth', block: 'center' });
            }, 300);
        });

        // 渲染旅程卡片
        function renderTrips() {
            const container = document.querySelector('.trips-container');
            container.innerHTML = trips.map(trip => `
                <div class="trip-card" onclick="enterTrip(${trip.id})">
                    <div class="trip-image" style="background-image: url('${trip.coverImage}')"></div>
                    <div class="trip-overlay">
                        <div class="trip-title">${trip.title}</div>
                        <div class="trip-meta">
                            <span>🌍 ${trip.country === 'japan' ? '日本' : trip.country === 'thailand' ? '泰國' : trip.country.toUpperCase()}</span>
                            <span>📅 ${new Date(trip.startDate).toLocaleDateString('zh-TW')} - ${trip.days}天</span>
                        </div>
                        <div class="trip-description">${trip.description}</div>
                    </div>
                </div>
            `).join('') + `
                <div class="add-trip-card" onclick="openAddTripModal()">
                    <div style="text-align: center;">
                        <div class="add-trip-icon">✨</div>
                        <div class="add-trip-text">+ 新增旅遊計劃</div>
                    </div>
                </div>
            `;
        }

        // 進入旅程詳情（暫時提示）
        function enterTrip(id) {
            const trip = trips.find(t => t.id === id);
            alert(`進入 ${trip.title} 詳情頁面\n\n天氣: ${trip.weather} [web:47]\n匯率: ${trip.currency}\n\n（完整功能頁面即將載入）`);
            // 這裡可以跳轉到對應旅程的詳細頁面
        }

        // 滾動到頂部
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // 開啟設置
        function openSettings() {
            alert('設置頁面功能即將推出！\n包含：主題顏色、字體大小、深色模式、飄落特效等');
        }

        // LocalStorage 保存/載入
        function saveTrips() {
            localStorage.setItem('travelTrips', JSON.stringify(trips));
        }

        function loadTrips() {
            const saved = localStorage.getItem('travelTrips');
            if (saved) {
                trips = JSON.parse(saved);
                renderTrips();
            }
        }

        // 初始化
        loadTrips();
        renderTrips();

        // 模態框點擊外部關閉
        document.getElementById('addTripModal').addEventListener('click', function(e) {
            if (e.target === this) {
                closeAddTripModal();
            }
        });

        // 添加飄落雪花特效
        function createSnowflake() {
            const snowflake = document.createElement('div');
            snowflake.className = 'snowflake';
            snowflake.innerHTML = '❄️';
            snowflake.style.left = Math.random() * 100 + 'vw';
            snowflake.style.animationDuration = (Math.random() * 3 + 2) + 's';
            snowflake.style.opacity = Math.random();
            document.body.appendChild(snowflake);

            setTimeout(() => {
                snowflake.remove();
            }, 5000);
        }

        setInterval(createSnowflake, 200);
    </script>
</body>
</html>
